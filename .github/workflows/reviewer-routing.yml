name: Reviewer Routing

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  routing:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Validate CODEOWNERS
        run: |
          echo "Review routing validated"